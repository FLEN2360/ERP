# -----------------------------
# Database Configuration
# -----------------------------

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# For Docker Compose, use the service name as host (e.g., postgres)
# For local development, use localhost as host (e.g., postgresql://postgres:postgres@localhost:5432/cmrdb_ams)
DATABASE_URL="postgresql://erp_user:erp_password@127.0.0.1:5432/erp_database"

# Individual PostgreSQL settings (used by docker-compose.yml)
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=erp_password
POSTGRES_DB=erp_database
POSTGRES_PORT=5432

# -----------------------------
# Application Configuration
# -----------------------------

# Node environment (development, production, test)
NODE_ENV=production

# Application port
APP_PORT=3000

# -----------------------------
# Nginx Configuration
# -----------------------------

# Nginx HTTP and HTTPS ports
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# -----------------------------
# Next.js Configuration
# -----------------------------

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# -----------------------------
# pgAdmin Configuration
# -----------------------------

# pgAdmin default login email
PGADMIN_DEFAULT_EMAIL=admin@example.com

# pgAdmin default login password
PGADMIN_DEFAULT_PASSWORD=admin

# pgAdmin web UI port
PGADMIN_PORT=5050
